Right click cmd icon > open as admin

type> pip install pprint
      pip install lxml
      pip install beautifulsoup4
      pip install requests
      
      navigate to the python folder

      type > python file_to_run.py